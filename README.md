# Swedish Hockey Stats Live Tracker

A beautiful, glassmorphism live hockey tracker built in pure HTML, CSS, and JS.
It fetches live hockey results dynamically from `stats.swehockey.se`.

## Running the Project Locally
Start a CORS proxy server:
```bash
python3 server.py
```
Open `http://localhost:8080` in your web browser.
